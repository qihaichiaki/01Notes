C# 方便的高级语言 -- 写起来舒服，但是运行速度不是很高。
Unity使用C#语言进行编程。


基本的变量类型：int、float、string、bool

条件语句 if else

循环语句 while、for

函数 返回类型、名称 (参数) {函数内容}

类 class 包含一系列变量和函数的东西。
实例化 new 类名();

结构体 struct  和类差不多（是和C++类似吗）

namepace  装很多的class... 使用using进行引用

访问权限：private（只有本类可访问） public（都可访问）

枚举、函数重载


数组
类型[] 变量名;  初始化数组 new 类型[长度]


继承
	从父类拿来的。
	
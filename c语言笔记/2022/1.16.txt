*补充循环流程图

dowhile 上来就干，之后判断还要不要继续执行
适应于至少循环一次的场景

算法适当，注意每次累乘或者累加时，要重新计算时，某些值要恢复成初值

*有序数组查找数字（二分查找，折半查找）
查找一次范围减少一半  查找效率极高，并且数组要有序的

找中法:right坐标加left坐标除二，进行比较，随后继续上次方法

查找数组元素 sizeof（arr）/sizeof（arr[0]）（在字符数组里面不可，有可能存在\0，所以应该用strlen。如果不用就要减去一，算下标就要减二了）

算平均值的最优法  两根柱子，变成一样高，一样高不就是平均值嘛  left +（right -left）/2

windows.h
Sleep (1000)单位是毫秒  休息一下
stdlib.h
system（"cls"）系统库函数（执行系统命令的和cmd执行命令差不多）

查找库函数网站   http://cplusplus.com/

两个字符串的比较是不能使用==，使用strcmp字符比较函数

*猜数字游戏1.电脑随机生成数字（1-100）
2.玩家猜数字
3.猜小了就告知猜小了，猜大了就告知猜大了
4.直到猜对为止
5.游戏可以一直玩
rand（） 库函数（生成一个随机数）（返回一个0-rand_max）头文件也是stdlib.h
要在这个之前要调用srand（无符号整型）这里的整型需要传递一个一直在发生变化的值（可以传电脑的时间）
时间戳
c语言提供了time（）里面是time_t类型地址，会返回time_t （类型） 整型    提供给srand需要强制转化类型
time(null)即不需要参数
注意srand不能频繁调用 一般一个工程调用一次，给一个起点即可
任何数字模上对应的几对应到0到几减一  模一百即 0-99  %100

goto（随意滥用，给个标记，然后跳转到该处）容易让代码编的凌乱
n:
goto n；
注意不能跨函数跳
在多层循环下好用

shutdown -s -t 设置秒数关机
shutdown -a 取消关机
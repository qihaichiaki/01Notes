c语言是结构化的程序设计语言

顺序   从头到尾
选择   出现选择  if  switch
循环   流转，重复，条件  while dowhile for

{}代码块

                            分支

*else与最近的if进行匹配（代码块除外{}）

*善于加{}，方便查看，不会误导人
代码风格很重要   善于加空格

*若return返回函数值则函数结束，如
if（a）
    return 0；
return  1;
这个就是具有选择性
好的代码风格   if(a)
                                 return 0;
                            else
                                 return 1;

*判断变量和常量把常量放在左边是个好的习惯

switch（整型表达式） case  整型常量表达式 : 只管从哪里进去（入口）
break决定出口
按需增加
*风格，最后一个break也要加上
default  都不符合走这里，也可加上break
若可以不用加也可加上（保证代码的完整性）
switch里面允许嵌套

                               循环
while（表达式）
continue

注意表达式假的条件和赋值语句是否执行

EOF的实现按Crtl z(在Windows环境下)

*scanf       缓冲区(按回车触发放在这里)   键盘  
 getchar
(键盘送入缓冲区，从缓冲区读)
scanf不会读取‘ ‘，getchar和putchar读取输出一个字符

空循环语句写法
{
     ；
}

for循环的使用  了解其变化规律（除此之外用其他的循环，谨防出错）

for调整时在括号里调整，别再循环体内调整，防止出bug
通常 建议判断语句前闭后开i=0，i＜10  增加可读性  适应环境，合适的场景
i=1，i＜=10

for循环的判断语句省略，判断恒为真（死循环）
*函数:子程序  由一个或者多个语句组成，完成某项特定的任务，具备相对的独立性。一般会有输入参数和返回值，可对过程的隐藏和细节的隐藏

库函数比如:printf scanf strlen
库函数存在的原因:早起代码冗余，开发效率低，不标准。
把一些常见的功能实现成函数，集成为库，由c语言直接提供。

const它限定一个变量不允许被改变，产生静态作用。

null（读文档时）\0
http://cplusplus.com/  学习库函数，使用库函数要包含头文件哦

strcpy拷贝时，也会把\0拷贝过去。
（string 数符串  copy复制）

size_t-＞ unsigned int（无符号的整型）x86

memset（改动的某个数组地址，改动的东西（整型），改动几个字节）

设置内存是以字节为单位的，
每个字节的内容都是一样的

函数基本描述:
返回类型 函数名 参数
函数体

f11调试转入函数

传给函数的参数  实参 可以是常量，变量，表达式，函数
形参是指函数括号中的形参，调用函数的时候才会开辟空间，函数结束释放。

传参方式:传值调用  传址调用

先写这个函数怎么用 （测试环境） 然后设计函数  tdd 测试驱动开发 test driven development

设计函数尽量功能简单，完成特定的任务！

数组传参的时候传递不是整个数组，而是首元素的地址。

printf返回类型是int，返回的是打印的几个字符就返回几。
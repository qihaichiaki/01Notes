gitee远程仓库
代码控制
git add *
git commit -m ""
删除 delete commit push
pull 拉取

文件后缀名.sln(解决方案,项目文件vs2019)

正片开始:
一个工程里面有且仅有一个main函数

枚举也是常量enum (一一列举 c语音提供的自定义类型的方法)枚举的大括号里面之间用，最后一个结束不需要； 为什么枚举常量这些事储存方式是常量即不可变的，和这个给这个枚举类型赋予的变量赋予其中的常量。

数组的方括号里面必须放常量哦（c99里面加了变长数组，可以使用变量，但不能定义同时赋值）

define标识符常量

大写快捷键ctrl d x  shift加字母该字母会大写

F10调试后，调试，窗口，监视可查看状态

ASCII码值 A 65，小写字母等于大写字母加32 0是48开始直到9。

\ddd ，1-3个八进制数 \xdd ，1-2个十六进制数

函数的作业 一次封装，多次调用

Linux Centos 7
vim 编译器
gcc c语言编译器

/操作符，两边都是整数，得到的是整数，若其中一边或者都是浮点数，则得到的是浮点数（小数）

%取模两边必须整数

移位操作符，位操作符都是操作的二进制位

赋值是已经初始化了的，初始化是未有值
 内置数据类型 char……_Bool
自定义类型:struct enum union 数组

全局变量作用域:整个工程 生命周期:程序共生

牛客网:oj题（online judge 在线判题）-笔试环节都是在线oj的形式  招工作:网申-＞笔试-＞面试（一面二面技术面，三面hr人力资源面试）-＞发offer   加油吧！
oj题型一般有两种形式:
1.IO型 代码全写
2.接口型 写部分函数（写核心程序）

EOF-end of file 文件结束标志
#define EOF -1

注意\ddd \xdd 这些均算一个字符

一开始分配空间就默认赋值为0，如全局变量

指针改实参

注意，scanf读取到几个数据，函数返回几，读取失败则返回EOF  -1（可以输入ctrl z 返回EOF）

双目操作符是两边都有操作数如a+b
单目操作符即只有一个操作数如!a

sizeof 计算变量和类型的所占空间的大小（字节为单位）

~ 2进制按位取反

不要过分的使用++  --  

（类型）将后面的强制类型转化，不会四舍五入，直接保留适应该类型的，其余直接扔掉（少用）

条件操作符（？:）可以简化书写，但是减少了程序的可读性，斟酌而行

 逗号表达式，从左到右依次计算，整个表达式的结果是最后一个表达式的结果（，，，……）

（）函数调用操作符 其中函数名是一个操作数，参数为其他的操作数

auto自动（修饰局部变量的）一般被省略了
extern声明外部变量
register 寄存器（寄存器（集成到CPU上的）byte  高速缓存器（cache）mb 内存g 硬盘g/t  网盘t）
static 修饰变量和函数（静态）
typedef类型重定义（把系统定义的类型重新起个名字-起别名）

CPU中央处理器-读写内存（早期）
现在 （两者速度差异大，造价差异大）cpu-（寄存器-高速缓存）-内存-＞提高了速度。

程序局部性--（深入理解计算机系统）

c语言关键字register建议将其放入寄存器（最终由编译器决定）寄存器变量是不能取地址的。（内存才有地址）
一般重命名类型这样写 
typedef struct stu stu
但是结构体也可这样
typedef struct stu
{
}stu；
和上述等价

结构体访问操作符
.结构体变量.结构体成员
-＞   结构体指针变量-＞结构体成员

strcpy字符拷贝函数

函数传参时，参数是需要压栈的
所以合适的用指针会极大的节省内存，提高效率。
*函数调用时，会在内存的栈区开辟空间的
栈区   局部变量    函数参数
堆区  动态内存开辟 malloc 
静态区 静态变量 全局变量
参数压栈   栈是一种数据结构（压栈=＞入栈）
类似于向箱子里面放东西，先放先进，后放先出
ed  add（a，b）先传参b，后传参a

*结构体传参时，传结构体的地址

bug臭虫，飞蛾 来源
调试 debugging debug  发现和减少bug。

发现程序错误的存在
1.程序员自己
2.测试人员-专门/专业bug
3.用户 -使用者

Debug调试版本
Releas发布版本(进行了各种优化，但是不提供调试)

注意快捷键f5是开始执行调试，但是不能单独使用一般和f9搭配使用。
f9切换断点/设置（取消）端点
停下来是逻辑上的停，而不是物理上的停
因此可以右键设置一个条件断点。
f10f11均是调试走一步，但是f11遇到函数就可进函数里面的语句。

遇到多重调用函数时，使用调用堆栈可以反应整个函数的调用逻辑。